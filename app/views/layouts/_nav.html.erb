<header class="brand_header">
  <% if not on_front_page? %>
    <a class="brand" href="/"><%= image_tag 'yumster_brand.png', :class => 'brand_image' %></a>
  <div class="brand_inner">
  <% else %>
  <div class="brand_inner_fp">
  <% end %>
    <ul class="brand_nav">
      <li><%= link_to 'About', main_app.pages_about_path %></li>
      <li class="divider-vertical"></li>
      <% if user_signed_in? %>
        <li><%= link_to main_app.user_path(current_user.username) do %><i class="icon-user icon-large"></i><% end %></li>
      <% else %>
        <li><%= link_to('Sign In', main_app.new_user_session_path) %></li>
      <% end %>
    </ul>
  </div>
</header>
