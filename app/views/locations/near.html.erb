<h2>Nearby Locations</h2>
<br>
<div class="row-fluid">
  <div class="span6 pull-right">
    <div class="input-append map_address_search">
      <input id="map_address_input" type="text" placeholder="Move to an address">
      <button id="map_address_button" class="btn" type="button">Go</button>
    </div>
    <div class="map_container">
      <div id="map_canvas"></div>
    </div>
    <a class="btn disabled" id="map_reload" href="#" onclick="window.Yumster.Locations.Near.searchHere()"><i class="icon-repeat"></i> Redo Search</a>
  </div>
  <div class="span6 pull-left no-left-margin">
    <ul id="nearby_results">
    </ul>
  </div>
</div>

<%= link_to "", near_locations_path, :id => "nearby_ajax_address" %>

<% content_for :scripts do %>
  <script src="https://maps.googleapis.com/maps/api/js?v=3&sensor=false"></script>
  <%= javascript_include_tag 'geolocationmarker.js' %>
  <script type="text/javascript" charset="utf-8">

// Load nearby locations
var map
var geolocationSuccess = function(latitude, longitude, coordinatesOnURL) {
  var loc = new google.maps.LatLng(latitude, longitude)
  map.setCenter(loc)
  window.Yumster.Locations.Near.fillNearbyLocations(latitude, longitude)
  if(!coordinatesOnURL) {
    window.Yumster.Locations.Near.updateURLLatLong(latitude, longitude)
  }
}

// Add user's location dot to map
var GeoMarker
var addPositionIndicator = function(map) {
  if(!GeoMarker) {
    GeoMarker = new GeolocationMarker()
    GeoMarker.setCircleOptions({fillColor: '#808080'})
    GeoMarker.setMap(map)
  }
}

// Initialize Google Maps v3 API
var init = function() {
  // Set up map
  map = new google.maps.Map(document.getElementById('map_canvas'), {
    zoom: 15,
    mapTypeId: google.maps.MapTypeId.ROADMAP
  })
  addPositionIndicator(map)
  window.Yumster.Locations.Near.setMap(map)

  var failure = function(error) {
    console.log("Error placing map: ", error)
  }
  window.Yumster.Locations.Near.getMapCenter(geolocationSuccess, failure)

  // On map idle, begin listening for map movement
  google.maps.event.addListenerOnce(map, 'idle', function() {
    google.maps.event.addListener(map, 'center_changed', function() {
      var center = map.getCenter()
      window.Yumster.Locations.Near.centerChanged(center.lat(), center.lng())
    })
  })
}
google.maps.event.addDomListener(window, 'load', init)

// Bind address search function to button
$(function() {
  window.Yumster.Locations.initializeAddressSearch(
    '#map_address_input',
    '#map_address_button',
    window.Yumster.Locations.Near.mapCallback)
})

  </script>
<% end %>
